# 5분 시초가 돌파 매매 자동매매 시스템 요구사항 정의서 (SRS) - v10 (완결판)

이 문서는 전일 주도주 중 장 초반의 의도적인 세력의 개미 털기(시가 이탈)를 포착하고, 시가를 재돌파할 때 강한 수급과 함께 안전하게 진입하는 **'5분 시초가 돌파 매매'** 전략을 시스템으로 자동화하기 위한 알고리즘 요구사항 정의서입니다 [1, 5, 9].

**v10 버전의 핵심 목표: 실전 승률 상위 1% 극대화 및 원금 초강력 보호**
본 버전은 v9의 무비용(0원) 호스팅 및 깃허브 자동 배포 아키텍처를 완벽히 계승하는 동시에, 실거래 백테스팅과 운용 시 계좌 파산을 방지하고 승률을 비약적으로 끌어올리기 위한 **3가지 실전 보호 필터(진입 제한 시간, 슬롯 기반 분할 자금 관리, 종합주가지수 필터)**를 전격 내장했습니다.

---

## 1. 무비용 (0원) 아키텍처 및 기술 스택 명세

실거래 시스템 구동 및 모니터링에 필요한 모든 기능을 비용 0원으로 구축하기 위해 다음과 같은 대안 기술을 설계했습니다:

| 역할 | 대안 기술 스택 (100% 무료) | 무비용 구현 방안 |
| :--- | :--- | :--- |
| **실시간 뉴스 검색** | **Naver 검색 Open API** | 네이버 개발자 센터에서 무료 API 키 발급 (일 25,000회 무료 쿼리 제공). 장중/장전 유니버스 10~15개 종목 수집에 충분함. |
| **AI 뉴스 요약 & 감성 분석** | **Google Gemini 1.5 Flash (Free Tier)** | Google AI Studio에서 무료 API 키 발급. **분당 15회(15 RPM), 일 1,500회(1500 RPD)** 무료 한도 제공. 4.5초 Throttling 요청을 통해 안전 구동. |
| **실시간 주식 시세 & 주문** | **국내 증권사 공식 API (KIS, 키움)** | 한국투자증권 KIS API v2 또는 키움증권 openAPI+ 사용. 실시간 시세(WebSocket) 및 주문 API 평생 무료 제공. |
| **텔레그램 알림 & 원격 제어** | **Telegram Bot API** | 텔레그램 공식 BotFather를 통한 로봇 개설 및 API 사용 평생 무료. |
| **데이터베이스** | **SQLite 3** | 로컬 파일 기반의 경량 오픈소스 RDBMS. 가상 서버(VM)의 RAM 점유율을 10MB 미만으로 억제. |
| **실행 및 배포 서버** | **Oracle Cloud Free Tier & GitHub** | Oracle Cloud VM(평생 무료, Ubuntu OS) 호스팅 배포. GitHub Actions를 통해 `auto_trade` 저장소 푸시 시 자동 무중단 빌드 연동. |

---

## 2. 매매 시스템 상태 정의 (System State Machine)

시스템은 대상 종목별로 독립적인 상태 머신(State Machine)을 운영하며, 자금 슬롯 부족 시 진입 보류 상태를 관리할 수 있도록 보완했습니다.

```
[STATE_IDLE] 
      │ (장 시작 전 종목 필터링 통과)
      ▼
[STATE_PRE_SCREENED] (08:50 1차 선정 및 무료 AI 뉴스 요약 진행)
      │ (09:00:00 장 시작 후 최초 확정 시가 수신 및 GAP_UP_PCT <= 5% 검증)
      ▼
[STATE_ACTIVE] (최종 실시간 매수 감시 상태)
      │ (RT_CURPRC가 DAY_OPNPRC 아래로 하락 ➔ 개미털기 확인)
      ▼
[STATE_WAIT_FOR_BREAKOUT (시가 이탈 상태)]
      │ (3분봉 완료 시점 시가 재돌파 수식, 실시간 수급 필터, 지수 필터, 자금 슬롯 검증 완료)
      ▼
[STATE_ORDER_SENT (주문 송신 및 체결 대기)] ──(미체결 15초 초과)──➔ [주문 취소 ➔ STATE_WAIT_FOR_BREAKOUT]
      │ (체결 완료 / 부분 체결 발생)
      ▼
[STATE_POSITION_HOLDING] 
      │ (실시간 목표가 도달 / 실시간 시가 재이탈 / 고정 손절선 이탈)
      ▼
[STATE_CLOSED] (당일 매매 완료 및 진입 제한)
```

---

## 3. 세부 알고리즘 및 기능 요구사항

### 3.1. [1단계] 장 시작 전 종목 필터링 및 유니버스 선정 (일봉 기준)
매 영업일 장 시작 전(08:30 ~ 08:50) 전일의 차트 및 거래 데이터를 분석하여 매매 대상 후보군을 자동 선정합니다.

* **조건 1: 돌파 패턴 식별 (다음 중 하나 이상 충족)**
  * **A. 상한가 종목**: 전 영업일 종가(`PREV_BIZDAY_DDBAR_CLSPRC`)가 가격제한폭(상한가)으로 마감된 종목 [1, 2].
  * **B. 저항선/신고가 돌파 종목**: 최근 박스권 상단 저항선(두꺼운 철문)을 돌파했거나, 전 영업일 고가(`PREV_BIZDAY_DDBAR_HGHPRC`)가 52주/120일 신고가를 돌파하여 상방에 매물 저항이 없는 종목 [2, 3].
* **조건 2: 돌파 당일 거래대금 검증**
  * 돌파를 완성한 캔들(전 영업일 일봉)의 당일 **총 거래대금(`PREV_BIZDAY_DDBAR_TRPRC`)이 500억 원 이상**인 종목만 선정 [4, 7].
* **[08:50] 1차 선별 완료 및 무비용 AI 뉴스 수집/요약 파이프라인 가동**
  * 1차 통과 종목에 대해 **Naver 뉴스 Open API**(무료)를 통해 관련 뉴스 검색 후 기사 원천 URL 확보.
  * **Google Gemini API Free Tier**를 활용하여 검색된 뉴스 원문 혹은 헤드라인을 바탕으로 🟢긍정/🟡중립/🔴부정 3단계 감성 분석(`NEWS_SENTIMENT`) 및 핵심 상승 재료 2줄 요약을 추출해 텔레그램 방으로 푸시 전송.

### 3.2. [2단계] 장 시작 시초가 동적 필터링 (09:00:00)
* **조건 3: 당일 시초가 갭(Gap) 비율 제한 (시간적 동적 판정 - v4 적용)**
  * 장 시작 시점에 수신된 확정 시초가(`DAY_OPNPRC`)와 전일 종가(`PREV_BIZDAY_DDBAR_CLSPRC`)를 연산하여 갭상승 비율(`GAP_UP_PCT`)이 **+5.0% 이하**인지 즉시 검증 [2].
  * **통과 종목**: 실시간 매수 대기 상태(`STATE_ACTIVE` ➔ `STATE_WAIT_FOR_DIP`)로 전이하고 감시 모듈 가동.
  * **탈락 종목**: 즉각 감시 목록에서 삭제 및 당일 거래 영구 제외(`STATE_CLOSED`) 처리 후 텔레그램에 자동 알림 전송.

### 3.3. [3단계] 실시간 매수 진입 타점 및 수급 검증 (분봉 기준)
실시간 감시 대상 종목에 대하여 다음의 가격 및 **수급 필터(S&D Filter)**를 동시에 만족할 때만 매수를 집행합니다.

* **기본 주기**: **3분봉** (가변 설정 가능) [4].
* **실시간 필터링 및 진입 알고리즘 수식 조건**:
  1. **시가 이탈 확인**: 장 시작 직후 실시간 가격(`RT_CURPRC`)이 당일 시초가(`DAY_OPNPRC`) 미만으로 하락하여 음봉 영역으로 떨어지는 흐름 관측 (`RT_CURPRC < DAY_OPNPRC`) [5].
  2. **시가 재돌파 3분봉 마감 조건**: 주가가 시초가 라인을 상향 돌파할 때, 해당 3분봉 캔들의 종가 마감 시점에서 다음 조건을 모두 만족해야 함 [5, 9]:
     * **조건 A**: 3분봉 종가(`CUR_M3BAR_CLSPRC`)가 당일 시초가(`DAY_OPNPRC`) 위에 위치하는 **양봉**일 것 [9].
     * **조건 B**: 돌파 시점 캔들의 몸통 상승률 `((CUR_M3BAR_CLSPRC - CUR_M3BAR_OPNPRC) / CUR_M3BAR_OPNPRC) * 100`이 **최소 2.0% 이상**일 것 [9].
     * **조건 C**: 돌파 캔들의 분봉 거래량(`CUR_M3BAR_TRAMT`)이 **최소 10,000주 이상** 터졌을 것 [9].
  3. **실시간 수급(S&D) 동시 필터링 검증**:
     * 3분봉 가격 돌파 시점에 실시간 수급 데이터인 **체결강도(`CUR_VOL_POWER`)가 100% 이상**이고, **호가잔량비(`ASK_BID_RATIO`)가 120% 이상**인 경우에만 최종 진입 승인. (가짜 돌파 휩소 방지)

### 3.4. [4단계] 청산 및 리스크 관리 (매도 규칙)
* **목표 익절가 (Take Profit)**: 매수 단가 대비 **최소 +5.0% 이상** 구간 도달 시 지정가 매도 주문 발주 [6].
* **실시간 손절가 (Stop Loss) - 틱단위 감시**:
  * **시가 재이탈 손절**: 진입 이후 실시간 가격(`RT_CURPRC`)이 당일 시초가(`DAY_OPNPRC`) 라인을 지지하지 못하고 하향 붕괴할 경우, **분봉 종가 마금을 기다리지 않고 즉시 시장가 즉각 청산** [5].
  * **최대 고정 손절**: 매수 단가 대비 **-3.0%** 도달 시 기계적 시장가 즉시 손절.

### 3.5. [5단계] 실전 승률 극대화 및 원금 보호 강화 필터 (신규 - v10 핵심)
실거래 환경에서 승률을 대폭 끌어올리고 대규모 폭락장에서 계좌를 수호하기 위해 다음 3가지 알고리즘 필터를 강제 집행합니다.

1. **신규 매수 진입 시간 제한 (Time-of-Day Filter)**:
   * **원칙**: 장전 스크리닝을 통과했더라도 **신규 매수 진입은 오직 아침 09:00:00부터 09:30:00까지만 허용**합니다.
   * **사유**: 거래량과 시장 참여자의 관심이 집중되는 장 초반 30분 이외의 시간대에 발생하는 시가 돌파는 가짜 휩소(Fake Breakout)일 확률이 매우 높으며 당일 우하향 횡보로 마칠 위험이 크기 때문입니다.
   * *주의: 09:30분 이전에 이미 진입 완료한 종목에 대한 실시간 익절/손절 청산 모듈은 장감 시점(15:30)까지 연속 가동됩니다.*
2. **슬롯(Slot) 기반 분할 자금 관리 규칙 (Money Management)**:
   * **원칙**: 시스템이 동시에 보유(Holding)할 수 있는 최대 종목 수를 **최대 3개(3슬롯)**로 제한합니다.
   * **투자 비율 할당**: 총 투자 가용 자금을 N분할(예: 3슬롯 배정 시 총액의 30%씩 할당)하여 한 종목에 예수금이 몰빵되는 현상을 완전 차단합니다. 
   * **진입 거부 장치**: 3개의 슬롯이 모두 가득 차 포지션을 홀딩 중일 때는, 타 종목에서 아무리 강한 3분봉 시가 돌파 및 수급 신호가 나와도 신규 매수 주문을 발송하지 않고 대기합니다.
3. **종합주가지수 필터 (Market Index Filter)**:
   * **원칙**: 당일 아침 시장 전체의 지수 상태를 진입 조건에 대입합니다. 
   * **세부 조건**: 코스피(`KOSPI_CHANGE_PCT`) 또는 코스닥(`KOSDAQ_CHANGE_PCT`) 지수의 전일 대비 수익률이 **-1.2% 이하로 폭락 출발하거나 폭락 중일 때**는 당일 자동매매 가동을 원천 취소 및 중단(Trading Block)합니다.
   * **사유**: 시장 전체가 패닉 투매 상태일 때는 아무리 강한 상한가 주도주도 시장 압력을 이기지 못하고 무너질 확률이 90% 이상이기 때문에, 진입하지 않고 현금을 보존하는 것이 최선의 자산 관리법입니다.

---

## 4. 데이터베이스 및 변수 매핑 테이블

| 변수명 | 데이터 타입 | 설명 | 획득 API 소스 | 비용 |
| :--- | :--- | :--- | :--- | :--- |
| **`PREV_BIZDAY_DDBAR_CLSPRC`** | Float | 전 영업일 일봉 종가 | 일봉 데이터 조회 API | **무료** |
| **`PREV_BIZDAY_DDBAR_HGHPRC`** | Float | 전 영업일 일봉 고가 | 일봉 데이터 조회 API | **무료** |
| **`PREV_BIZDAY_DDBAR_TRPRC`** | Float | 전 영업일 일봉 거래대금 (원) | 일봉 데이터 조회 API | **무료** |
| **`DAY_OPNPRC`** | Float | 당일 확정 시초가 | 실시간 시세 / 분봉 Open 가격 | **무료** |
| **`RT_CURPRC`** | Float | 실시간 현재가 | 실시간 체결 시세 API | **무료** |
| **`CUR_M3BAR_OPNPRC`** | Float | 현재 3분봉 캔들의 시가 | 3분봉 데이터 API | **무료** |
| **`CUR_M3BAR_CLSPRC`** | Float | 현재 3분봉 캔들의 종가 | 3분봉 데이터 API | **무료** |
| **`CUR_M3BAR_TRAMT`** | Integer | 현재 3분봉 캔들의 거래량 | 실시간 분봉 거래량 수집 | **무료** |
| **`GAP_UP_PCT`** | Float | 갭 상승률 | 자체 연산 | **무료** |
| **`CUR_VOL_POWER`** | Float | 실시간 체결강도 (%) | 실시간 체결 수신 TR API | **무료** |
| **`ASK_BID_RATIO`** | Float | 실시간 총 호가잔량비 (%) | 실시간 호가 수신 TR API | **무료** |
| **`INST_NET_BUY`** | Float | 기관 실시간 순매수 가집계 (원) | 당일 주체별 가집계 조회 API | **무료** |
| **`FRGN_NET_BUY`** | Float | 외국인 실시간 순매수 가집계 (원) | 당일 주체별 가집계 조회 API | **무료** |
| **`NEWS_SENTIMENT`** | String | 뉴스 감성 라벨 (`POSITIVE` 등) | Google Gemini API (Free Tier) | **무료** |
| **`KOSPI_CHANGE_PCT`** | Float | 코스피 지수 전일대비 변동률 (%) | 업종 실시간 지수 조회 API | **무료** |
| **`KOSDAQ_CHANGE_PCT`** | Float | 코스닥 지수 전일대비 변동률 (%) | 업종 실시간 지수 조회 API | **무료** |

---

## 5. 텔레그램 양방향 원격 제어 및 알림 규격 (무료 운영)

### 5.1. 시스템 알림 (Push 알림)
1. **[08:50 예비 유니버스 알림]**: 1차 선발 종목 리스트, 전일 거래대금, **AI 긍부정 뉴스 요약 내용** 송신.
2. **[09:00 종합 지수 및 시가 갭 판정 알림]**: 당일 종합지수 현황(`KOSPI_CHANGE_PCT`, `KOSDAQ_CHANGE_PCT`) 및 갭 비율 통과/탈락 명단 송신. 시장 폭락(-1.2% 이하) 시 자동 가동 정지 상황 즉각 경고 보고.
3. **[실시간 체결 알림]**: 돌파 진입 시 체결가, 체결수량, 당시 실시간 수급 지표(체결강도, 호가잔량비) 즉각 보고.
4. **[실시간 청산 알림]**: 익절/손절 청산 결과 및 최종 확정 수익률(%) 전송.

### 5.2. 사용자 원격 조작 명령어 (Command API)
* `/status`: 시스템 구동 상태, 감시 대상 종목 현황, 사용 중인 자금 슬롯 개수(`active_slots_count`), 보유 종목의 실시간 평가손익(%) 일괄 조회.
* `/news [종목명]`: 해당 종목의 최신 호재 기사 즉시 스크롤 및 AI 긍부정 요약본 호출.
* `/supply_demand [종목명]` (또는 `/sd [종목명]`): 해당 종목의 실시간 체결강도, 호가잔량비, 외인/기관 순매수액 리얼타임 리포팅.
* `/re_screen`: 장중 실시간 스크리닝 파이프라인 수동 즉시 강제 기동 및 감시 유니버스 전면 갱신.
* `/force_sell [종목명]`: 특정 종목 즉각 전량 시장가 매도 및 감시 강제 종료.
* `/stop_all`: 긴급 SOS 전체 중지. 모든 감시 스레드를 차단하고 보유 잔고를 가장 유리한 호가에 긴급 매도 처리.

---

## 6. 실거래 빈번 발생 이슈 및 방지 설계 규칙 (Slippage & Rate Limits)

1. **지정가 초과(Limit Offset) 주문**: 슬리피지 방지를 위해 돌파 가격 마감가 대비 **+2틱 높은 지정가 주문**을 발송하여 과도한 높은 가격에 호가가 꼬여 체결되는 것을 방어합니다.
2. **미체결/부분체결 자동 제어**: 매수 전송 후 15초 미체결 시 자동 취소 처리를 가동하며, 부분체결 수량에 대해서만 즉각적인 리스크 관리(시가 재이탈 감시)를 독립 가동합니다.
3. **무료 API 트래픽 제어 (Rate Limiter)**:
   * **Google Gemini API**: Free tier가 제공하는 **15 RPM** 한도를 초과하지 않도록, 장 전(08:50)에 1차 통과된 종목들의 뉴스 분석을 수행할 때 종목 간 **최소 4.5초의 간격(Delay)**을 두어 비동기 쿼리를 전송합니다. 이로써 무료 호출 한도 초과 오류(429 Too Many Requests)를 완벽히 피합니다.
4. **당일 1회 진입 잠금**: 잦은 휩소에 의한 수수료 누적 방지를 위해 청산이 완료된 종목(`STATE_CLOSED`)은 장중 재감시 대상에서 완벽히 배제합니다.
5. **시간대 제한 자동 리셋**: 09:30 이후에는 신규 진입 스레드만 꺼지며, 청산 스레드는 유지됩니다. 15:30 장 마감 이후에는 전량 자금 슬롯 반환 및 내일 매매 준비를 위해 전 종목의 상태 머신이 `STATE_IDLE`로 자동 복구됩니다.
